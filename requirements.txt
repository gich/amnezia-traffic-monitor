# TOML loader: tomllib is in stdlib on 3.11+, tomli is the same API as a backport.
tomli>=2.0; python_version < "3.11"

# Web UI dependencies:
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.9
